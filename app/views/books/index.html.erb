<h1>Books#index</h1>
<p>Find me in app/views/books/index.html.erb</p>
<%= @book.each do |book| %>
  <h3><%= link_to book.bookname, book_path(book.id) %></h3>
<% end %>
<%= link_to "Add Book", new_book_path %>
